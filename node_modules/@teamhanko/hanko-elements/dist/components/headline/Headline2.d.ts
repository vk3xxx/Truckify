import { ComponentChildren } from "preact";
type Props = {
    children: ComponentChildren;
};
declare const Headline2: ({ children }: Props) => import("preact").JSX.Element;
export default Headline2;
