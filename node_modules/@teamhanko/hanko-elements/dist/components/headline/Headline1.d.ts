import { ComponentChildren } from "preact";
type Props = {
    children: ComponentChildren;
};
declare const Headline1: ({ children }: Props) => import("preact").JSX.Element;
export default Headline1;
