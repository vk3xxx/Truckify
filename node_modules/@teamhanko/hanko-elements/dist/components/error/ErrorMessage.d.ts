import { FlowError } from "@teamhanko/hanko-frontend-sdk";
interface Props {
    flowError?: FlowError;
}
declare const ErrorMessage: ({ flowError }: Props) => import("preact/compat").JSX.Element;
export default ErrorMessage;
