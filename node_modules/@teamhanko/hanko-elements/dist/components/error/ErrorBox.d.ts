import { FlowError, HankoError, State } from "@teamhanko/hanko-frontend-sdk";
type Props = {
    state?: State<any>;
    flowError?: FlowError;
    error?: HankoError;
};
declare const ErrorBox: ({ state, error, flowError }: Props) => import("preact/compat").JSX.Element;
export default ErrorBox;
