import { ComponentChildren } from "preact";
interface Props {
    children?: ComponentChildren;
    hidden?: boolean;
}
declare const Divider: ({ children, hidden }: Props) => import("preact").JSX.Element;
export default Divider;
