import { ComponentChildren } from "preact";
type Props = {
    hidden?: boolean;
    children: ComponentChildren;
};
declare const Paragraph: ({ children, hidden }: Props) => import("preact").JSX.Element;
export default Paragraph;
