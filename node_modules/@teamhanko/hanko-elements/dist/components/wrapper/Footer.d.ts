import { ComponentChildren } from "preact";
interface Props {
    hidden?: boolean;
    children?: ComponentChildren;
}
declare const Footer: ({ children, hidden }: Props) => import("preact").JSX.Element;
export default Footer;
