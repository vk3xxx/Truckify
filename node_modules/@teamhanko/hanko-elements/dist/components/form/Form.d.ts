import { ComponentChildren } from "preact";
import { Action } from "@teamhanko/hanko-frontend-sdk";
type Props = {
    onSubmit?: (event: Event) => void;
    children: ComponentChildren;
    hidden?: boolean;
    maxWidth?: boolean;
    flowAction?: Action<any>;
};
type FormContextType = {
    flowAction?: Action<any>;
};
export declare const FormContext: import("preact").Context<FormContextType>;
export declare const useFormContext: () => FormContextType;
declare const Form: ({ onSubmit, children, hidden, maxWidth, flowAction, }: Props) => import("preact").JSX.Element;
export default Form;
