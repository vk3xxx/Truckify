import { h } from "preact";
interface Props {
    passcodeDigits: string[];
    numberOfInputs?: number;
    onInput?: (passcodeDigits: string[]) => void;
    disabled?: boolean;
}
declare const CodeInput: ({ passcodeDigits, numberOfInputs, onInput, disabled, }: Props) => h.JSX.Element;
export default CodeInput;
