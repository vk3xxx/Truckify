import { h } from "preact";
import { Props as LoadingSpinnerProps } from "../icons/LoadingSpinner";
import { Action } from "@teamhanko/hanko-frontend-sdk";
type LoadingSpinnerPosition = "left" | "right";
export interface Props extends LoadingSpinnerProps, h.JSX.HTMLAttributes<HTMLButtonElement> {
    onClick?(event: Event): void;
    dangerous?: boolean;
    loadingSpinnerPosition?: LoadingSpinnerPosition;
    flowAction?: Action<any>;
}
declare const Link: ({ loadingSpinnerPosition, dangerous, onClick, flowAction, ...props }: Props) => h.JSX.Element;
export default Link;
