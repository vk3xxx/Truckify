import { h } from "preact";
import { StateUpdater } from "preact/compat";
import { State } from "@teamhanko/hanko-frontend-sdk";
interface Props {
    checkedItemID?: string;
    setCheckedItemID: StateUpdater<string>;
    flowState: State<"profile_init">;
    onState(state: State<any>): Promise<void>;
}
declare const ChangeUsernameDropdown: ({ checkedItemID, setCheckedItemID, flowState, onState, }: Props) => h.JSX.Element;
export default ChangeUsernameDropdown;
