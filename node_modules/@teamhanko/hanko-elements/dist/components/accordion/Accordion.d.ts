import { h } from "preact";
import { StateUpdater } from "preact/compat";
type Selector<T> = (item: T, itemIndex?: number) => string | h.JSX.Element;
interface Props<T> {
    name: string;
    columnSelector: Selector<T>;
    contentSelector: Selector<T>;
    checkedItemID?: string;
    setCheckedItemID: StateUpdater<string>;
    data: Array<T>;
    dropdown?: boolean;
}
declare const Accordion: <T>({ name, columnSelector, contentSelector, data, checkedItemID, setCheckedItemID, dropdown, }: Props<T>) => h.JSX.Element;
export default Accordion;
