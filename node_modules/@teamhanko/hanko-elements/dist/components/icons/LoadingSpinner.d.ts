import { ComponentChildren } from "preact";
export type Props = {
    children?: ComponentChildren;
    isLoading?: boolean;
    isSuccess?: boolean;
    fadeOut?: boolean;
    secondary?: boolean;
    hasIcon?: boolean;
    maxWidth?: boolean;
};
declare const LoadingSpinner: ({ children, isLoading, isSuccess, fadeOut, secondary, hasIcon, maxWidth, }: Props) => import("preact").JSX.Element;
export default LoadingSpinner;
