import { State, WebauthnCredential } from "@teamhanko/hanko-frontend-sdk";
type Props = {
    oldName: string;
    credential: WebauthnCredential;
    credentialType: "passkey" | "security-key";
    onBack: (event: Event) => Promise<void>;
    onCredentialNameSubmit: (event: Event, id: string, name: string) => Promise<void>;
    flowState: State<"profile_init">;
};
declare const RenameWebauthnCredentialPage: ({ onCredentialNameSubmit, oldName, onBack, credential, credentialType, flowState, }: Props) => import("preact").JSX.Element;
export default RenameWebauthnCredentialPage;
