import { State } from "@teamhanko/hanko-frontend-sdk";
interface Props {
    state: State<"profile_init">;
    enablePasskeys?: boolean;
}
declare const ProfilePage: (props: Props) => import("preact").JSX.Element;
export default ProfilePage;
