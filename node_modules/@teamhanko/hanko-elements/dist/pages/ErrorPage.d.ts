import { State, HankoError } from "@teamhanko/hanko-frontend-sdk";
interface Props {
    state?: State<any>;
    error?: HankoError;
}
declare const ErrorPage: ({ state, error }: Props) => import("preact/compat").JSX.Element;
export default ErrorPage;
